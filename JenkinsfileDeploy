pipeline {
  agent any
  stages {
    stage('Undeploy') {
      steps {
        sh 'cd /docker/cmp && docker-compose down || true'
      }
    }
    stage('Deploy') {
      steps {
        sh 'cd /docker/cmp && docker-compose up -d'
      }
    }
  }
}
